apiVersion: v1
kind: Pod
metadata: 
  name: pod-demo
  label:
    app: nginx
    demo: service
spec:
  contianers:
    - name: nginx
      image: nginx
      port:
      - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: service-demo
spec:
  selector: 
    app: nginx
    demo: service
  port:
    - protcol: TCP
      port: 80
      targetPort: 80 
